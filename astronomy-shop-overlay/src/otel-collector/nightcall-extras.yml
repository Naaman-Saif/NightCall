receivers:
  http_check/frontend-proxy:
    collection_interval: 10s
    targets:
      - endpoint: http://frontend-proxy:8080
      - endpoint: http://frontend-proxy:8080/api/recommendations?productIds=OLJCESPC7Z&currencyCode=USD
