# Paste this into the claude-big session once NightCall is pushed

You are on the Night Call demo box. The astronomy shop is already running as compose project `prod` with your `compose.box-override.yaml`. Your job: get Night Call running next to it and drive one incident end to end. Write everything to /root/code/NOTES-box.md as you go, under a new heading "Phase 1 to 5". Never touch ssh, cloudflared, ufw or systemd units. Never edit files under /root/code/astronomy-shop except to add the overlay files named below; upstream files stay untouched.

Coding rules if you change anything in /root/code/night-call: read BRIEF.md "Coding rules" first. Zero comments, max 20 lines per function, 2 params, 2 indent levels, 100 lines per file. `npm run lint` enforces most of it. Commit each fix separately with a one-line message, in /root/code/NightCall, and `git push` at the end.

1. `git clone git@github.com:NaamanSaif/NightCall.git /root/code/NightCall && ln -sfn /root/code/NightCall/night-call /root/code/night-call && cd /root/code/night-call && cp .env.example .env && npm install`. If the clone needs credentials, say so and stop. Read README.md, ARCHITECTURE.md and the last three sections of NOTES.md.
2. `.env`: set `NIGHT_CALL_GITHUB_REPOSITORY=NaamanSaif/NightCall`. For AWS: if `~/.aws/credentials` or `AWS_ACCESS_KEY_ID` already exists on this box, copy the key pair into `.env`; otherwise write "AWS credentials needed in /root/code/night-call/.env" as the first line of your report and continue with everything that does not need Bedrock (steps 4 to 7), then stop before step 8. Same for `GITHUB_TOKEN`: if a token with issues:write is not available, note it and continue; the pipeline will fail only at the last step. For `NIGHT_CALL_MODEL_ID`: `pip install awscli` (or apt install awscli), then `aws bedrock list-inference-profiles --region eu-central-1 --query "inferenceProfileSummaries[].inferenceProfileId"` and pick the newest Claude Sonnet profile. Record the exact string.
3. `npm run smoke:bedrock`. If it fails on credentials, stop and say what is missing. If it fails on anything else, read the error, check node_modules/@strands-agents/sdk/dist/src/models/bedrock.d.ts, fix scripts/smoke-bedrock.ts, retry.
4. Overlay: `npm run overlay -- /root/code/astronomy-shop`, then from /root/code/NightCall/astronomy-shop-overlay copy `compose.nightcall.yaml` into /root/code/astronomy-shop/, `src/alertmanager/` into /root/code/astronomy-shop/src/, and `src/prometheus/rules/` into /root/code/astronomy-shop/src/prometheus/. Set `NIGHT_CALL_PATH=/root/code/night-call` and `ASTRONOMY_SHOP_PATH=/root/code/astronomy-shop` in /root/code/astronomy-shop/.env.override.
5. `docker compose --env-file .env --env-file .env.override -p prod -f compose.yaml -f compose.full.yaml -f compose.observability.yaml -f compose.box-override.yaml -f compose.nightcall.yaml config > /dev/null && echo prod-config-ok`, same with `-p clone` and `compose.clone.yaml` instead of nightcall. Fix generation bugs in scripts/make-overlay.ts if either fails.
6. `make -C /root/code/night-call prod-up SHOP=/root/code/astronomy-shop`. Confirm: `docker logs night-call` shows "Nest application successfully started"; `curl -s localhost:9090/api/v1/rules | grep -c night-call` is 1; `curl -s localhost:9093/-/ready`; `curl -s localhost:8001/` returns the status page.
7. Wait for `docker exec night-call cat /state/snapshots/last-green.json`. If it never appears within 3 minutes, run `docker exec night-call node -e "require('/app/dist/probes/probes.service.js')"` and then `cd /root/code/night-call && npm run signature -- prod` from inside the container (`docker exec -w /app night-call npx tsx scripts/signature.ts prod` may not have tsx; if so read the failing probe from `docker logs night-call` debug lines). Fix the probe that is wrong. Likely suspects, in order: the Jaeger URL, Prometheus metric names, a container in `watchedServices` that does not exist in this stack, the cart/checkout request shape.
8. `make -C /root/code/night-call break-payment SHOP=/root/code/astronomy-shop`. Follow `docker logs -f night-call`. Expect within ~8 minutes: alert received, incident opened, triage hypothesis, clone up, reproduced, trials, issue filed with a URL. Record the wall time of each step.
9. If the run fails partway: read the error, fix the smallest thing, `make heal-payment`, wait for a new green snapshot, and repeat step 8. Up to three attempts. Tear down any leftover clone with `docker compose -p clone down --volumes --remove-orphans`.
10. When an issue has been filed: paste its URL and the full body into NOTES-box.md, then `make heal-payment`. Then run the same for `break-ad` / `heal-ad`.
11. Final section of NOTES-box.md: what worked first time, what you changed (with commit hashes), what is still wrong, and the exact timings. `git push` your commits.
